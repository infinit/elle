#!/usr/bin/env python3

import os
import subprocess
import sys

arch, osyst, compiler = os.environ['BUILDFARM_NAME'].split('-')
os.chdir('%s' % os.environ['DIR_BUILD'])
if subprocess.call(['python3', 'drake', '//build'], environ=os.environ) != 0:
  exit(1)
