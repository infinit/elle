#pragma once

namespace elle
{
  namespace protocol
  {
    class Channel;
    class ChanneledStream;
    class BaseRPC;
    template <typename ISerializer, typename OSerializer>
    class RPC;
    class Serializer;
    class Stream;
  }
}
