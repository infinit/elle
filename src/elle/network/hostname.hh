#ifndef ELLE_NETWORK_HOSTNAME_HH
# define ELLE_NETWORK_HOSTNAME_HH

# include <string>

# include <elle/compiler.hh>

namespace elle
{
  namespace network
  {
    ELLE_API
    std::string
    hostname();
  }
}

#endif
