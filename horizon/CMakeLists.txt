PROJECT(Facade CXX)

ADD_LIBRARY(facade
  Facade.cc
)

IF(APPLE)
  ADD_SUBDIRECTORY(unix)

  TARGET_LINK_LIBRARIES(facade unix)
ELSEIF(UNIX)
  ADD_SUBDIRECTORY(unix)

  TARGET_LINK_LIBRARIES(facade unix)
ELSEIF(WIN32)
  ADD_SUBDIRECTORY(windows)

  TARGET_LINK_LIBRARIES(facade windows)
ENDIF()
