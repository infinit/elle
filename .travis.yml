sudo: required
language: c
compiler: gcc
services:
  - docker
script:
  - cd _build/linux64
  - make check
  - make sample
  - ls -lR
  - ./src/sample test
after_success:
  - bash <(curl -s https://codecov.io/bash)
