--

experiments:
 o differentes taille de cache depot.
 o ne garder dans le cache depot que les PKB pour voir si ca change l'impact.

--

Report doit etre thread-safe. Utiliser le Local Storage.
QT fournit ce qu'il faut!
