dans la struct RSA, garder les autres champs speciaux car ils sont utilises
pour une decryption plus rapide (je crois)

--

dans Region et d'autres objets, a chaque methode, checker si l'objet
est deja cree, si c'est le cas appeler Delete() et effectuer l'operation.

--

operator<<

afficher un truc du genre "chb#[hash]"

--

Report doit etre thread-safe: compiler avec -DREENTRANT

* pthread_getspecific()--Get Thread Local Storage Value by Key
* pthread_key_delete()--Delete Thread Local Storage Key
* pthread_setspecific()--Set Thread Local Storage by Key 

 http://en.wikipedia.org/wiki/Thread-Local_Storage
suce
